[project]
name = "jkr-campaign-worker"
version = "0.1.0"
description = "JKR AI Calling — campaign dialer loop: safety gate, dispatch, retries (docs/SECURITY_AND_COMPLIANCE.md §2)"
requires-python = ">=3.12,<3.13"
dependencies = [
  "jkr-db",
  "jkr-messaging",
  "dramatiq[redis]>=1.17.1",
  "pydantic-settings>=2.5.2",
  "httpx>=0.27.2",
]

[tool.uv.sources]
jkr-db = { workspace = true }
jkr-messaging = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[dependency-groups]
dev = [
  "pytest>=8.3.3",
  "pytest-asyncio>=0.24.0",
]
